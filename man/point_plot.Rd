% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/point_plot.R
\name{point_plot}
\alias{point_plot}
\title{Course-friendly replacement for LSTbook::point_plot when annot = "model"}
\usage{
point_plot(
  D,
  tilde,
  ...,
  annot = c("none", "violin", "model", "bw"),
  interval = c("confidence", "none", "prediction"),
  jitter = 0.15,
  point_alpha = 0.7,
  smooth_method = "lm"
)
}
\arguments{
\item{D}{A data frame.}

\item{tilde}{A formula like \code{y ~ x}, \code{y ~ x + z}, or \code{y ~ x + z + w}.}

\item{...}{Passed to \code{LSTbook::point_plot()} when \code{annot != "model"}.}

\item{annot}{Annotation mode; only \code{"model"} triggers the custom behavior.}

\item{interval}{Kept for compatibility; ignored when \code{annot = "model"}.}

\item{jitter}{Jitter width used when x is categorical.}

\item{point_alpha}{Point transparency.}

\item{smooth_method}{Method passed to \code{geom_smooth()} (default \code{"lm"}).}
}
\value{
A ggplot object.
}
\description{
In the annot = "model" branch this function avoids LSTbook::point_plot()
and uses simple ggplot rules for common teaching cases.
}
\details{
Change requested: in the y ~ x + z + w case, draw ONLY ONE smooth line per facet
(ignoring z in the fit), while still coloring points by z.

When \verb{annot = "model" and interval = "none"}, this function avoids \code{LSTbook::point_plot()} and uses
simplified ggplot logic for common course cases. Otherwise,
it delegates to \code{LSTbook::point_plot()}.
}
